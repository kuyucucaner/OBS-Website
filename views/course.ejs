<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Courses</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@10.16.6/dist/sweetalert2.min.css">
    <link rel="stylesheet" href="./stylesheets/course.css">
    <link rel="stylesheet" href="./stylesheets/navbar.css">
</head>
<body>
    <%- include('../views/partials/navbar') %>
    <div class="courses">
        <h1>Dersler</h1>
        <table border="1">
            <thead>
                <tr>
                    <th>Ders Adı</th>
                    <th>Ders Açıklaması</th>
                    <th>Dersin Hocası</th>
                    <th>Materyal Dosyası</th>
                    <th>Ders  Dönemi</th>
                    <th>Ders Derecesi</th>
                    <% if (user.role === 'Öğrenci') { %>
                        <th>Dersi Ekle</th>
                        <% } %>
                 
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < courses.length; i++) { %>
                    <tr>
                        <td><%= courses[i].CourseName %></td>
                        <td><%= courses[i].CourseDescription %></td>
                        <td><%= courses[i].TeacherName %></td>
                        <td><%= courses[i].Title %></td>
                        <td><%= courses[i].Semester %></td>
                        <td><%= courses[i].Grade %></td>
                        <% if (user.role === 'Öğrenci') { %>
                            <td>
                              <!-- Form Action Attribute -->
                              <form id="enrollForm_<%= i %>" action="/selectCourse/<%= user.ID %>" method="POST">
                                <input type="hidden" name="course" value="<%= courses[i].ID %>">
                                <button type="submit" class="btn btn-success">Dersi Ekle</button>
                            </form>                            
                            </td>
                        <% } %>
                    </tr>
                <% } %>
            </tbody>        
        </table>
    </div>
    <script src='./javascripts/selectCourse.js'></script>
    <script src='./javascripts/goToPages.js'></script>
    <script src='./javascripts/logout.js'></script>    
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10.16.6/dist/sweetalert2.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
