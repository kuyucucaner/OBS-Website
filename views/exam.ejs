<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exams</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@10.16.6/dist/sweetalert2.min.css">
    <link rel="stylesheet" href="./stylesheets/exams.css">
    <link rel="stylesheet" href="./stylesheets/navbar.css">
</head>
<body>
    <%- include('../views/partials/navbar') %>
    <div class="exams">
    <h1>Sınavlar</h1>
    <table border="1">
        <thead>
            <tr>
                <th>Ders Adı</th>
                <th>Ders Açıklaması</th>
                <th>Tarih</th>
                <th>Materyal Dosyası</th>
            </tr>
        </thead>
        <tbody>
            <% for (let i = 0; i < exams.length; i++) { %>
                <tr>
                    <td><%= exams[i].CourseName %></td>
                    <td><%= exams[i].CourseDescription %></td>
                    <td>Tarih: <%= new Date(exams[i].Date).toLocaleDateString('tr-TR') %> || Saat: <%= new Date(exams[i].Date).toLocaleTimeString('tr-TR', { timeZone: 'Europe/Istanbul', hour12: false, hour: '2-digit', minute: '2-digit' }) %></td>
                    <td><%= exams[i].Title %></td>
                </tr>
            <% } %>
        </tbody>        
    </table>
</div>
    <script src='./javascripts/goToPages.js'></script>
    <script src='./javascripts/logout.js'></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10.16.6/dist/sweetalert2.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
